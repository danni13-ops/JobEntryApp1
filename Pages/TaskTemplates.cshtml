@page
@model JobEntryApp.Pages.TaskTemplatesModel

<h1>Task Templates</h1>

<p class="text-muted">
    This page shows the template rows that generate job tasks.
</p>

<table class="table table-striped">

    <thead>
        <tr>
            <th>Order</th>
            <th>Task Name</th>
            <th>Stage</th>
            <th>Offset Days</th>
            <th>Anchor</th>
        </tr>
    </thead>

    <tbody>

        @if (Model.TaskTemplates != null)
        {
            foreach (var task in Model.TaskTemplates)
            {
                <tr>
                    <td>@task.SortOrder</td>
                    <td>@task.TaskName</td>
                    <td>@task.Stage</td>
                    <td>@task.OffsetDays</td>
                    <td>@task.AnchorType</td>
                </tr>
            }
        }

    </tbody>

</table>
